services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  rate-limiter:
    build: .
    ports:
      - "8080:5000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - PORT=5000
      - FLASK_DEBUG=0
    restart: unless-stopped
